name: Repository Dispatch Event

on:
  repository_dispatch:
    types: [an-event]

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Say Something
        run: echo The Rest API told me to do it!
